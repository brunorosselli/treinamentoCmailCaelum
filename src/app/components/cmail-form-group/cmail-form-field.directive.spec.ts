import { CmailFormFieldDirective } from './cmail-form-field.directive';

describe('CmailFormFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new CmailFormFieldDirective();
    expect(directive).toBeTruthy();
  });
});
